## What & why

What does this change and what problem does it solve? Link any related issue.

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Detection tuning (threshold/heuristic change in `src/config.ts`)
- [ ] Docs / chore

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (added/updated tests for the change)
- [ ] `npm run build` succeeds
- [ ] No new **runtime** dependency (dev-only is fine)
- [ ] Updated `README.md` / `CHANGELOG.md` if behavior changed

## Detection-tuning note (if applicable)

If this changes a threshold or heuristic, what's the effect on the validation
corpus (`npm run validate`)? Recall / false-positive before → after.

## How I verified
